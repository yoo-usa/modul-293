{{ loadCssComponent('banner') }}

<div class="banner" style="background-image: url({{ bannerImage }})">
    <span class="banner__intro">{{ intro }}</span>
</div>
<div class="banner--fake"></div>

{{ content }}
